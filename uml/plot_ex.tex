┌───────────────┐
│ DataManager    │  ← ログを保存する（あなたが作ったクラス）
└───────┬───────┘
        │
        │ speed_data.txt / training_data.txt
        ▼
┌───────────────┐
│ Plotter        │  ← ログを読み取ってグラフを作る
│  (plotter.py)  │
└───────┬───────┘
        │
        │ PNGとして保存
        ▼
┌──────────────────────────┐
│ グラフ画像（PNG）         │
│ ・wpm_over_time.png       │
│ ・training_scores.png     │     
└──────────────────────────┘


ログを読む
   ↓
必要な値を計算する（WPMや正答数）
   ↓
日付単位にまとめる（平均 or 合計）
   ↓
直近90日だけに絞る
   ↓
Matplotlibでグラフを書く
   ↓
PNGで保存する